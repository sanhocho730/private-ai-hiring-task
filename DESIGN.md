# DESIGN

**원칙: 모델이 보지 못한 내용은 새어 나갈 수 없다.** 권한 제어를 LLM에 맡기지 않고, LLM에게 보여줄 문서를 코드가 정한다.

```
질문 → 임베딩(bge-m3) 유사도 → [권한 필터] 열람 가능 / 초과 문서 분리
  ├ 열람 가능 & 관련(≥0.50) → 프롬프트 → 답변(exaone3.5:2.4b) + 코드가 고른 원문 출처
  └ 초과 → 본문 폐기, (등급, 점수)만 → 힌트 규칙 → 힌트가 붙으면 답변 없이 고정 문장만
```

## 분류: 규칙 + LLM, 둘이 다르면 높은 등급 (9/9)
규칙은 보안 표식·민감 키워드(`[기밀]`, `내부용`, `위약금` 등)를, LLM(qwen2.5:3b)은 내용을 본다. 규칙만으로는 표식 없는 문서를 놓치고 LLM만으로는 판단이 흔들린다. 낮게 매기면 유출, 높게 매기면 답을 덜 할 뿐이라 높은 쪽을 택했다. 실제로 LLM은 05 계약 문서를 O로 매기지만 규칙이 S로 올린다. 정답 라벨은 채점 스크립트에서만 읽는다.

## 유출 방지
`ask()`는 유사도를 구한 직후 문서를 `visible`/`hidden`으로 나누고, 프롬프트와 출처는 `visible`로만 만든다. `hidden`에는 `(등급, 점수)`만 남으므로 **초과 문서 본문이 LLM에 들어가는 경로가 없다.** 문서에 프롬프트 인젝션이 있어도 모델은 권한 밖 내용을 본 적이 없다. 힌트가 붙으면 과제 문구대로 **힌트만** 준다. LLM 답변과 출처를 생략해 모델이 상위 내용을 추측할 여지도 없앴다.

## 힌트: 임베딩 점수 + 고정 문장 (LLM 미관여)
| 초과 문서 | 조건 | 출력 (`config.py` 상수) |
|---|---|---|
| S | 점수 ≥ 0.55 | "상위 등급 문서에 관련 내용이 있어 답이 달라질 수 있습니다. 팀장에게 확인하세요." |
| C | S 사용자만, 점수 ≥ 0.46, 열람 문서가 관련(≥0.50)하고 C보다 점수가 높음 | "상위 등급에서 관련 검토가 진행 중인 정황이…" |

C는 **존재 자체가 기밀**이라 C 문서만 가리키는 질문("어디 인수한대?")에 힌트를 주면 그것이 존재 확인이 된다. 그래서 질문의 주된 주제가 볼 수 있는 문서일 때만 알리고, O 사용자에게는 알리지 않는다(2번 → S 힌트, 9번 → C 힌트, 4·8번 → 없음). "우선순위가 바뀔 수 있다" 같은 주제 표현은 그 단어가 상위 문서 내용에서 나오므로 쓰지 않았다.

## 모델 선택: 2019 Intel 맥, 8GB RAM 안에서
qwen2.5:7b는 스왑 때문에 첫 질문도 11분 안에 끝나지 않았다. qwen2.5:3b는 분류 9/9지만 답변에 한자가 섞였고(`90일前`), exaone3.5:2.4b는 한국어 답변이 깔끔하지만 분류 7/9였다. 그래서 **분류는 qwen, 답변은 EXAONE**으로 나눴다.

## 검증: 막았다가 아니라 막혔는지 확인
- **유출:** `check_leaks.py`가 볼 수 없는 문서에만 있는 숫자·단어를 출력과 대조한다(라벨 기준). 결과 0건이며, 가짜 유출을 넣어 검사기 자체도 확인했다.
- **재현성:** temperature 0, 고정 seed. 캐시와 모델을 비우고 두 번 실행해도 출력이 같았다.
- **채택 기준:** 답변 모델 교체 후에는 12개와 추가 17개 질문에서 나빠진 답이 없을 때만 변경을 반영했다.

## 실패한 시도
**① 유사도 크기로 직접/간접 질문을 가르려 했다.** C 점수가 아주 높으면 침묵, 중간이면 힌트를 주려 했지만 실측값은 4번 0.48(침묵), 8번 0.51(침묵), 9번 0.48(힌트)로 갈리지 않았다. 임베딩은 주제의 거리만 재고 묻는 방식은 담지 않는다. 그래서 **열람 가능 문서의 최고 점수**(4번 0.32, 8번 0.38, 9번 0.59)를 조건으로 바꿨다.

**② 프롬프트 규칙으로 작은 모델의 답을 고치려 했다.** 7번의 만료일 누락을 고칠 때마다 다른 답이 무너졌고, 9번은 C 내용 쪽으로 추측했으며, "결론을 예/아니오로 먼저"는 "예" 한 단어짜리 오답을 낳았다. 결국 가상 문서 예시 하나로 답의 모양만 보여주고, 정답 원문은 코드가 출처로 붙인다.

## 한계와 확장
임계값은 12개 질문 분포로 정해 C 힌트 경계(6번 0.44 / 9번 0.48)의 여유가 작다. 문서가 많아지면 등급을 메타데이터로 두고 벡터 검색의 필터 조건으로 거는 것이 같은 원칙의 확장이다. 네트워크는 `127.0.0.1:11434`(Ollama)만 호출한다.
